// Shared framework-agnostic helper functions used across apps/backend and apps/frontend.
// Intentionally empty for now — populated as real shared logic is needed
// (e.g. Haversine distance calculation) so both apps stay in sync on one definition.
export {};
