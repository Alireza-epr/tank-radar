// Shared enums used across apps/backend and apps/frontend.
// Intentionally empty for now — populated as real shared enums are needed
// (e.g. radius options, sort direction) so both apps stay in sync on one definition.
export {};
